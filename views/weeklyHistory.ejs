<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Histórico Semanal</title>
  <!-- Tailwind CSS via CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-100">

  <section class="containerHeader">
    <nav class="relative px-4 py-4 flex justify-between items-center bg-slate-800 mb-8">
      <a class="text-3xl font-bold leading-none" href="/">
        <img class="w-48" src="/images/logo.svg" alt="Logo">
      </a>
      <div class="lg:hidden">
        <!-- Botão do menu hamburguer -->
        <button id="navbarBurger" class="navbar-burger flex items-center text-blue-600 p-3">
          <svg class="block h-6 w-6 fill-current" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
            <title>Menu Mobile</title>
            <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"></path>
          </svg>
        </button>
      </div>
      <!-- Menu principal para telas grandes -->
      <ul class="hidden absolute top-1/2 left-1/2 transform -translate-y-1/2 -translate-x-1/2 lg:flex lg:mx-auto lg:items-center lg:w-auto lg:space-x-6">
        <li><a class="text-sm text-gray-400 hover:text-gray-500 font-bold" href="/">Início</a></li>
        <li class="text-gray-300">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" class="w-4 h-4" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 5v0m0 7v0m0 7v0"/>
          </svg>
        </li>
        <li><a class="text-sm text-white font-bold" href="/weeklyHistory">Doações</a></li>
        <li class="text-gray-300">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" class="w-4 h-4" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 5v0m0 7v0m0 7v0"/>
          </svg>
        </li>
        <li><a class="text-sm text-gray-400 hover:text-gray-500 font-bold" href="#">Histórico</a></li>
        <li class="text-gray-300">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" class="w-4 h-4" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 5v0m0 7v0m0 7v0"/>
          </svg>
        </li>
        <li><a class="text-sm text-gray-400 hover:text-gray-500 font-bold" href="#">Temporadas</a></li>
        <li class="text-gray-300">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" class="w-4 h-4" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 5v0m0 7v0m0 7v0"/>
          </svg>
        </li>
        <li><a class="text-sm text-gray-400 hover:text-gray-500 font-bold" href="#">Contato</a></li>
      </ul>
    </nav>
  
    <!-- Menu lateral para mobile -->
    <!-- Removemos o "hidden" e aplicamos classes para transição suave -->
    <div id="navbarMenu" class="navbar-menu fixed inset-0 z-50 transform -translate-x-full opacity-0 transition duration-300 ease-in-out">
      <!-- Fundo semi-transparente -->
      <div class="navbar-backdrop fixed inset-0 bg-gray-800 opacity-0 transition duration-300 ease-in-out"></div>
      <nav class="fixed top-0 left-0 bottom-0 flex flex-col w-5/6 max-w-sm py-6 px-6 bg-white border-r overflow-y-auto">
        <div class="flex items-center mb-8">
          <a class="mr-auto text-3xl font-bold leading-none" href="#">
            <!-- Exemplo de logo SVG -->
            <img class="w-48" src="/images/logo.svg" alt="Logo">
          </a>
          <!-- Botão para fechar o menu -->
          <button id="navbarClose" class="navbar-close">
            <svg class="h-6 w-6 text-gray-400 cursor-pointer hover:text-gray-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
        <div>
          <ul>
            <li class="mb-1">
              <a class="block p-4 text-sm font-semibold text-gray-400 hover:bg-blue-50 hover:text-blue-600 rounded" href="/">Início</a>
            </li>
            <li class="mb-1">
              <a class="block p-4 text-sm font-semibold text-gray-400 hover:bg-blue-50 hover:text-blue-600 rounded" href="#">About Us</a>
            </li>
            <li class="mb-1">
              <a class="block p-4 text-sm font-semibold text-gray-400 hover:bg-blue-50 hover:text-blue-600 rounded" href="#">Services</a>
            </li>
            <li class="mb-1">
              <a class="block p-4 text-sm font-semibold text-gray-400 hover:bg-blue-50 hover:text-blue-600 rounded" href="#">Pricing</a>
            </li>
            <li class="mb-1">
              <a class="block p-4 text-sm font-semibold text-gray-400 hover:bg-blue-50 hover:text-blue-600 rounded" href="#">Contact</a>
            </li>
          </ul>
        </div>
        <div class="mt-auto">
          <p class="my-4 text-xs text-center text-gray-400">
            <span>Copyright © 2021</span>
          </p>
        </div>
      </nav>
    </div>
  </section>

  <div class="container mx-auto p-6">
    <h1 class="text-3xl font-extrabold mb-8 text-center">Histórico Semanal</h1>
    <% if (weeklyHistory && weeklyHistory.length > 0) { %>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <% weeklyHistory.forEach(item => { %>
          <div class="bg-gray-800 p-6 rounded-lg shadow-lg transform hover:scale-105 transition duration-300">
            <div class="flex justify-between items-center border-b border-gray-700 pb-3 mb-4">
              <div>
                <h2 class="text-2xl font-bold">Semana <%= item.weekNumber %></h2>
                <p class="text-sm text-gray-400"><%= new Date(item.date).toLocaleString() %></p>
              </div>
              <div class="text-right">
                <p class="font-semibold text-lg">Total Semanal: <%= Number(item.totalDonations).toLocaleString('pt-BR', { minimumFractionDigits: 0, maximumFractionDigits: 2 }) %></p>
                <p class="font-semibold text-lg">Média Semanal: <%= item.averageDonation %></p>
              </div>
            </div>
            <div>
              <p class="font-bold mb-2">Top doadores:</p>
              <ol class="list-decimal list-inside">
                <% item.topDonors.forEach(function(donor, index) { 
                     let medal = '';
                     if (index === 0) { medal = '🥇'; }
                     else if (index === 1) { medal = '🥈'; }
                     else if (index === 2) { medal = '🥉'; }
                %>
                  <li class="mb-1">
                    <span class="mr-2 text-xl"><%= medal %></span>
                    <span class="font-medium"><%= donor.name %></span>
                    <span class="text-sm text-gray-300">(<%= Number(donor.donations).toLocaleString('pt-BR', { minimumFractionDigits: 0, maximumFractionDigits: 2 }) %> doações)</span>
                  </li>
                <% }); %>
              </ol>
            </div>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <p class="text-center text-lg">Nenhum histórico semanal registrado ainda.</p>
    <% } %>
    <!-- Botão de reset (se necessário) -->
    <!--
    <div class="mt-8 text-center">
      <button id="resetAllButton" class="bg-red-600 hover:bg-red-500 text-white font-bold py-2 px-6 rounded-full shadow-md transition duration-300"> 
        Resetar Tudo
      </button>
    </div>
    -->
  </div>

  <!--  
  <script>
    document.getElementById('resetAllButton')?.addEventListener('click', async () => {
      try {
        const response = await fetch('/resetAll');
        const data = await response.json();
        alert(data.message);
        window.location.reload();
      } catch (error) {
        console.error("Erro ao resetar tudo:", error);
      }
    });
  </script> -->

  <!-- Caso utilize arquivo externo, mantenha a chamada -->
  <script src="js/main.js"></script>
</body>
</html>
